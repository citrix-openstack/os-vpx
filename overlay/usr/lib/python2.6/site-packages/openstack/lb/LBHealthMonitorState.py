from openstack.lb.State import State


class LBHealthMonitorState(State): 

    def __init__(self, lbresource):
        State.__init__(self, lbresource)
        self._properties = ["type", "delay", "timeout", "attemptsBeforeDeactivation", "path", "statusRegex", "bodyRegex"]
