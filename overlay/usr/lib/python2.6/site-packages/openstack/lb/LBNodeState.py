from openstack.lb.State import State


class LBNodeState(State): 

    def __init__(self, lbresource):
        State.__init__(self, lbresource)
        self._properties = ["id", "address", "port", "condition", "weight", "status"]
