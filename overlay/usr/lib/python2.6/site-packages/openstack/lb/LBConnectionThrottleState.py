from openstack.lb.State import State


class LBConnectionThrottleState(State): 

    def __init__(self, lbresource):
        State.__init__(self, lbresource)
        self._properties = ["minConnections", "maxConnections", "maxConnectionRate", "rateInterval"]
