from openstack.common.ServiceFaultException import ServiceFaultException

class ImmutableEntityException(ServiceFaultException):

    def __init__(self, message):
        ServiceFaultException.__init__(self, 422, message)


    def get_as_xml(self):

        output = "<immutableEntity xmlns=\"" + self.xml_namespace_uri + "\" code=\"" + str(self.statuscode) + "\">"

        output += "<message>" + self.message + "</message>"

        output += "</immutableEntity>"

        return output


    def get_as_json(self):

        output = "{" 
        output +=      "\"immutableEntity\"" + ":"
        output +=                "{"
        output +=                    "\"code\"" + ":" + "\"" + str(self.statuscode) + "\","
        output +=                    "\"message\"" + ":" + "\"" + self.message + "\""      
        output +=                "}"
        output += "}" 

        return output
