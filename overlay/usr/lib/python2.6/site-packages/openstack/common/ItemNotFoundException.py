from openstack.common.ServiceFaultException import ServiceFaultException

class ItemNotFoundException(ServiceFaultException):

    def __init__(self, message):
        ServiceFaultException.__init__(self, 404, message)


    def get_as_xml(self):

        output = "<itemNotFound xmlns=\"" + self.xml_namespace_uri + "\" code=\"" + str(self.statuscode) + "\">"

        output += "<message>" + self.message + "</message>"

        output += "</itemNotFound>"

        return output


    def get_as_json(self):

        output = "{" 
        output +=      "\"itemNotFound\"" + ":"
        output +=                "{"
        output +=                    "\"code\"" + ":" + "\"" + str(self.statuscode) + "\","
        output +=                    "\"message\"" + ":" + "\"" + self.message + "\""      
        output +=                "}"
        output += "}" 

        return output
